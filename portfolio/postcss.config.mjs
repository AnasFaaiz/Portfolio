/* Tailwind v4: the plugin lives in its own package and there is no
   `tailwindcss` PostCSS plugin entry any more. This is the whole file. */
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
